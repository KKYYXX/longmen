<!--pages/zcquery_detail/zcaquery_detail.wxml-->
<view class="detail-container">
  <!-- 文件信息卡片 -->
  <view class="file-card">
    <view class="file-header">
      <view class="file-icon">📄</view>
      <view class="file-info">
        <view class="file-name">{{fileName}}</view>
        <view class="file-type">{{fileType}}</view>
      </view>
    </view>
    
    <view class="file-details">
      <view class="detail-item">
        <text class="label">文件路径：</text>
        <text class="value">{{fileUrl}}</text>
      </view>
      
      <view class="detail-item" wx:if="{{isLocalFile}}">
        <text class="label">文件位置：</text>
        <text class="value local-file">本地文件</text>
      </view>
    </view>
  </view>

  <!-- 操作按钮 -->
  <view class="action-buttons">
    <button class="action-btn preview-btn" bindtap="previewFile">
      <text class="btn-icon">👁️</text>
      <text class="btn-text">预览文件</text>
    </button>
    
    <button class="action-btn download-btn" bindtap="downloadFile">
      <text class="btn-icon">⬇️</text>
      <text class="btn-text">下载文件</text>
    </button>
    
    <button class="action-btn share-btn" bindtap="shareFile">
      <text class="btn-icon">📤</text>
      <text class="btn-text">分享文件</text>
    </button>
    
    <button class="action-btn copy-btn" bindtap="copyFilePath">
      <text class="btn-icon">📋</text>
      <text class="btn-text">复制路径</text>
    </button>
  </view>

  <!-- 提示信息 -->
  <view class="tips-section" wx:if="{{isLocalFile}}">
    <view class="tips-header">
      <text class="tips-icon">ℹ️</text>
      <text class="tips-title">本地文件说明</text>
    </view>
    <view class="tips-content">
      <text>此文件存储在本地计算机上，无法直接预览或下载。如需访问文件，请使用本地文件管理器打开对应路径。</text>
    </view>
  </view>
</view>